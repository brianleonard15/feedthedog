
/* Generic redirect function. This will take a location to redirect *
 * the browser to and then send the user there.                     */
function redirect(location)
{
	window.location = "/" + location + "/";
}